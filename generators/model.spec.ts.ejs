import {testSapi} from '../../spec/helpers/sakura-api';
import {<%=className%>} from './<%=rawFileName%>';

describe('<%=className%> tests', () => {
  const sapi = testSapi({
    models: [<%=className%>]
  });

  it('<%=className%>', () => {
    pending('not implemented');
  });
});
