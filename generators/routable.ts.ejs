import {
  Routable,
  Route,
  SakuraApi,
  SapiRoutableMixin
} from '@sakuraapi/core';
import {
  NextFunction,
  Request,
  Response
} from 'express';

export { SakuraApi };

@Routable()
export class <%=className%> extends SapiRoutableMixin() {

  @Route()
  someRoute(req: Request, res: Response, next: NextFunction) {
    next();
  }
}
